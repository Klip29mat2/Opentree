export default class TreeCompiler {

    compile(nodes, edges) {

        console.log("Nodes :", nodes);
        console.log("Edges :", edges);

        const tree = {
            trunklength: 50,
            trunkradius: 2
        };

        return tree;
    }

}